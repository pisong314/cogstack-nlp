version: '3.7'

services:
  deidentify:
    container_name: deidentify
    build:
      context: .
      dockerfile: Dockerfile
    command: python manage.py runserver 0.0.0.0:8000

    ports:
      - "8000:8000"
