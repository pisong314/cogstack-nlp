version: '3.7'

services:
  deidentify-medcat1-10:
    build:
      context: .
      dockerfile: Dockerfile
    command: ./run.sh
    env_file:
      - ./anoncat/.env
    volumes:
      - ../models:/home/models
    restart: unless-stopped
    ports:
      - "8004:8000"
